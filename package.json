{
  "name": "jeremiah-anthony-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portfolio — static site, no build step, no runtime dependencies.",
  "scripts": {
    "dev": "python -m http.server 8080 --bind 127.0.0.1",
    "dev:node": "npx --yes serve . -l 8080"
  }
}
